# ───────────────────────────── Android / Gradle ─────────────────────────────
*.iml
.gradle/
/local.properties
/build
/captures
.externalNativeBuild
.cxx

# Per-module build outputs
**/build/
app/release/
app/debug/

# Generated APK / AAB / mapping
*.apk
*.aab
*.ap_
*.dex
output.json

# ───────────────────────────── IDE: Android Studio / IntelliJ ────────────────
# IDE config is developer-specific and should not be committed.
.idea/

# ───────────────────────────── Kotlin / compiler ────────────────────────────
.kotlin/

# ───────────────────────────── Signing & secrets ────────────────────────────
# Never commit real signing material or credentials.
# A redacted template is committed as keystore.properties.example instead.
keystore.properties
*.keystore
*.jks
/keystore/

# ───────────────────────────── OS / editor noise ────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Stray vim write-quit artifact (see repo history)
wq

# ───────────────────────────── Logs ─────────────────────────────────────────
*.log

# NOTE: gradle/wrapper/gradle-wrapper.jar IS intentionally committed so the
# Gradle wrapper works on a fresh clone. Do not ignore it.
